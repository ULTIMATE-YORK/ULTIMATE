<configuration>
  
  <!-- TextArea Appender for JavaFX -->
  <appender name="TEXTAREA" class="logging.TextAreaAppender">
    <!-- You can optionally add a layout here if needed -->
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- Root Logger -->
  <root level="INFO">
    <appender-ref ref="TEXTAREA" />
  </root>
</configuration>